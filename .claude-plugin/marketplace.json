{
  "name": "vdsmon-skills",
  "metadata": {
    "description": "Personal Claude Code plugins by vdsmon. Each skill ships as its own plugin so you can pick and choose. Plugins prefixed with cc- are Claude-Code-specific (hooks, dynamic context injection, or ${CLAUDE_SKILL_DIR}); unprefixed plugins port cleanly to other SKILL.md-native hosts (OpenAI Codex, Gemini CLI, Cursor, Goose, Copilot, etc.)."
  },
  "owner": {
    "name": "Victor De Simone",
    "email": "victorsmoliveira@gmail.com",
    "url": "https://github.com/vdsmon"
  },
  "plugins": [
    {
      "name": "skill-polish",
      "source": "./plugins/skill-polish",
      "description": "Post-mortem for any skill — scans a session for friction and applies concrete edits to the responsible skill file. Portable across SKILL.md-native hosts.",
      "version": "1.2.0"
    },
    {
      "name": "cc-tokenomics",
      "source": "./plugins/cc-tokenomics",
      "description": "Token usage, cache hit rates, and Max plan consumption analyzer. /cc-tokenomics dashboard + reference docs on rate limits, cache lifecycle, and empirical billing experiments. Claude Code only.",
      "version": "2.0.1"
    },
    {
      "name": "cc-cache-keepalive",
      "source": "./plugins/cc-cache-keepalive",
      "description": "Keeps Claude Code's prompt cache warm on Max plans via a SessionStart-scheduled silent cron. Opt-in via ~/.cc-cache-keepalive flag file. Cron prompt is sentinel 'cc-cache-keepalive' — gate Stop hooks on it to silence turn-end sounds. Claude Code only (hook + cron).",
      "version": "1.2.2"
    },
    {
      "name": "pre-compact",
      "source": "./plugins/pre-compact",
      "description": "Audit in-flight state before any context-compacting step; produce a copy-paste /compact message plus a queue-able follow-up that chains the next action when compact finishes. Portable across Agent Skills hosts — compacting is a general concept, not Claude-specific.",
      "version": "1.5.0"
    },
    {
      "name": "humanize",
      "source": "./plugins/humanize",
      "description": "Strip AI-writing tells from text. Detects em-dash overuse, AI vocabulary, inflated significance, rule-of-three, sycophancy, and 20+ more patterns. Portable across SKILL.md-native hosts.",
      "version": "1.3.0"
    },
    {
      "name": "dg",
      "source": "./plugins/dg",
      "description": "Adversarial code review — two Agent-tool personas (Gilfoyle attacks, Dinesh defends) debate the diff and converge on an actionable verdict. Portable (agent dispatch varies by host).",
      "version": "1.1.0"
    },
    {
      "name": "converge",
      "source": "./plugins/converge",
      "description": "Run a prompt or slash command in a loop until changes converge or start churning. Each pass runs in a fresh subagent for impartial review. Portable (subagent dispatch varies by host).",
      "version": "1.1.0"
    },
    {
      "name": "ship-it",
      "source": "./plugins/ship-it",
      "description": "Push branch, open draft PR, attach reviewers, then wait on CI and the AI code reviewer in a fix loop until green. Ticket-agnostic. Supported stack today: Bitbucket Cloud + bkt CLI + CodeRabbit. Adapter slots reserved for gh + other reviewer bots.",
      "version": "0.3.1"
    },
    {
      "name": "tasks",
      "source": "./plugins/tasks",
      "description": "Filesystem-based two-phase task orchestration. /tasks:spec breaks an epic into atomic stories with shell-runnable acceptance + dep graph; /tasks:orchestrate dispatches the ready set in parallel-safe batches via subagents until done. Bootstraps project templates on first use. Portable (subagent dispatch varies by host).",
      "version": "0.2.0"
    },
    {
      "name": "team-lead",
      "source": "./plugins/team-lead",
      "description": "Coordinating a team of Agent-spawned teammates shipping a multi-story epic. Atomic-imperative dispatch, lead-mediated commit serialization (v2 + v3 shared-file ownership), wave composition via file-conflict matrix, sandbox-approval handling, failure recovery. Universal playbook — portable across projects; per-project lane assignments stay in project memory.",
      "version": "0.1.0"
    },
    {
      "name": "adx-loop",
      "source": "./plugins/adx-loop",
      "description": "Agent Development Experience self-improvement loop. Each cycle: ship prior retro's queued harness fix → ship a gameplay feature that pressure-tests the surface → retro (top pain + one queued fix) → log a single composite metric. Karpathy autoresearch discipline: one change per iteration, mechanical metric, git as memory, binary keep/discard. Optimizes the dev environment, not the product.",
      "version": "0.1.0"
    },
    {
      "name": "rapidfire",
      "source": "./plugins/rapidfire",
      "description": "Dispatcher pattern. User drops one-line ideas; the dispatcher refines in ≤3 questions, writes a ticket to `.rapidfire/T<NN>-<slug>.md`, fire-and-forget dispatches to a background subagent. Auto-selects model + agent_type via a 4-bucket complexity heuristic (trivial→haiku/cavecrew-builder, moderate→sonnet/general-purpose, complex/ambiguous→opus/general-purpose). Pre-dispatch lint catches T01-class spec defects (acceptance grep contradicting an intended edit). Listens for `<task-notification>` blocks, auto-dispatches queued tickets when their `depends_on` is satisfied. Lead reviews + commits via `/rapidfire commit`. Helper scripts (status/lint-spec/dispatch-args/migrate/optimize-description) are uv PEP 723 single-file Python (no global deps). Portable across SKILL.md-native hosts (subagent dispatch varies).",
      "version": "0.1.0"
    }
  ]
}
